using UnityEngine;

namespace Common
{
	[CreateAssetMenu(fileName = CreateMenus.vector2EventFile, menuName = CreateMenus.vector2EventMenu,
		order = CreateMenus.vector2EventOrder)]
	public sealed class Vector2Event : BaseEvent<Vector2>
	{
		
	}
}