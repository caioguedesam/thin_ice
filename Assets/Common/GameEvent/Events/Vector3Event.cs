using UnityEngine;

namespace Common
{
	[CreateAssetMenu(fileName = CreateMenus.vector3EventFile, menuName = CreateMenus.vector3EventMenu,
		order = CreateMenus.vector3EventOrder)]
	public sealed class Vector3Event : BaseEvent<Vector3>
	{
		
	}
}